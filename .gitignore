# Logs
*.log
npm-debug.log*

# Dependencies / build
node_modules
dist
dist-ssr
coverage
*.local
.vite

# 秘密。★ アクセス解析の読み取りトークンなどをここに置く。絶対にコミットしない。
#   例は .env.example にある（そちらは値を持たないので追跡する）。
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python のバイトコード（scripts/ 配下のツールが吐く）
__pycache__/
*.pyc

# wl214: 生成物（走者の全Pt系列。DB から再生成できる）
scripts/wl214/report.json
scripts/wl214/shifts.json

# wl214: 走者の名義（章ごとに改名する）。公開リポジトリに入れない
scripts/wl214/.runner
