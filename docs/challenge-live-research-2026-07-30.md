# チャレンジライブ 調査結果（2026-07-30）

役割: チャレンジライブのスコア計算・編成・選曲に関する調査の保管庫。ツール実装の一次資料。
出典: Web調査（攻略サイト・有志検証note・OSS実装）＋ マスタDB／`music_metas.json` の実データ検証
関連: [mysekai-measurement-2026-07-27.md](mysekai-measurement-2026-07-27.md)

---

## 0. この文書の要点

**攻略記事を読む必要はほぼない。** 判定係数・コンボ係数・レベル係数・ノーツ重みは、すべて**マスタDBに一次データとして入っている**。記事の孫引きではなくそこを読むこと。

**チャレンジライブのスコア計算はソロライブと完全に同一。** 違いは「同一キャラ縛り」と「スキル発動回数＝編成枚数+1」の2点だけ。フィーバーなし、活躍ボーナスなし。

---

## 1. スコア計算式

### 1ノーツあたり

```
1ノーツのスコア
  = (総合力 × 4 ÷ 重み付きノーツ総数)
    × ノーツ重み × 判定係数 × レベル係数 × コンボ係数 × スキル効果

総スコア = floor( Σ(各ノーツ) + 活躍ボーナス )
```

`重み付きノーツ総数` は譜面全ノーツの `scoreCoefficient` の総和。**分子のノーツ重みと分母が相殺するため、ノーツ数はスコアに直接効かない。**

別表記として `基礎スコア = 総合力 × (3.9 + 譜面Lv ÷ 50)` があるが、`4 × レベル係数` を展開したものと数学的に同一。

```
4 × (1 + (L-5)×0.005) = 3.9 + L/50
```

### 高速版（曲まるごと近似・実用にはこちらで十分）

```
rate  = base_score + Σ_{i=0..5} ( scoreUp_i × skill_score_solo[i] ÷ 100 )
score = floor( rate × 総合力 × 4 )
```

`base_score` と `skill_score_solo`（長さ6の配列）は `music_metas.json` に楽曲×難易度ごとに入っている。**スキル発動位置の実際の価値が反映済み**なので、「5秒×6回÷曲長」のような近似より正確。

ライブ種別ごとの基礎レート:

| 種別 | baseRate |
|---|---|
| **SOLO / CHALLENGE** | `base_score` |
| MULTI / CHEERFUL | `base_score + fever_score × 0.5` |
| AUTO | `base_score_auto` |

---

## 2. 係数テーブル（マスタDBの実値）

配信元: `https://sekai-world.github.io/sekai-master-db-diff/*.json`（CORS全開・1日数回更新）

### 判定係数 `ingameNoteJudges.json`

| 判定 | 係数 | ライフ減 |
|---|---|---|
| perfect | 1.0 | 0 |
| great | **0.7** | 0 |
| good | 0.5 | 0 |
| bad | 0.0 | 50 |
| miss | 0.0 | 100 |
| dead | 0.7 | 0 |
| auto | **0.7** | 0 |

※ 一部の攻略サイトが「AUTO=50%」としているが、マスタデータ上は 0.7。

### コンボ係数 `ingameCombos.json`

100コンボごとに +0.01、**1001以上で 1.10 打ち止め**（全11段）。
適用は「そのノーツを叩いた時点のコンボ数」なので、**曲の後半ほど1ノーツの価値が高い**。

### レベル係数 `ingamePlayLevels.json`

```
Lv1〜5  : 1.000（一律）
Lv6以降 : 1.000 + (Lv − 5) × 0.005
Lv26 = 1.105 / Lv30 = 1.125 / Lv40 = 1.175（上限）
```

**EASY/NORMAL/… という難易度区分ごとの係数は存在しない。** 効くのは `playLevel`（数値）だけ。

### ノーツ重み `ingameNotes.json`

| 種別 | 重み |
|---|---|
| normal / long端点 / flick | 10.0 |
| long_mid（ロング中間） | 1.0 |
| **normal_critical / long_critical（金）** | **20.0** |
| **flick_critical（金フリック）** | **30.0** |
| long_mid_critical | 2.0 |
| friction（トレース）系 | 1.0（flickのみ10.0、critical系2.0） |

**金フリック1個 = 白タップ3個分。** スキル区間に金ノーツが密集する譜面が有利になる理由がこれ。

### スキル `skills.json`

例（純スコアアップ）: Lv1 +20% / Lv2 +25% / Lv3 +30% / Lv4 +40%、持続 **5.0秒**、発動判定 `good` 以上。

### チャレンジライブ `challengeLives.json`

```json
[{"id":1,"playableCount":1,"challengeLiveDetails":[{"id":1,"challengeLiveType":"solo"}]}]
```

**1日1回、種別は `solo`。** 関連: `challengeLiveCharacters.json`（キャラ解放条件）、`challengeLiveStages.json`（ステージ報酬）、`challengeLiveHighScoreRewards.json`（スコア閾値報酬）。

### イベント側の上限（参考）

- `eventTotalPowerLimits.json`: イベント202/205/207/211 で **総合力上限 336,000**
- `eventSkillScoreUpLimits.json`: イベント180 で **スキル倍率上限 240%**

---

## 3. チャレンジライブ固有の仕様

| 項目 | 内容 |
|---|---|
| 挑戦回数 | 1日1回、毎朝4時リセット |
| 回数の消費 | **クリアした時点**。リタイア・リトライでは消費しない |
| 編成 | 選択キャラ1人のカードのみ、最大5枚 |
| 編成枠の解放 | キャラランク **13 / 18 / 22** で追加（最大5枠） |
| スキル発動回数 | **編成枚数 + 1**（5枚なら6回） |
| 6回目 | **リーダーのアンコール確定** |
| フィーバー | なし（係数1.0） |
| 活躍ボーナス | なし（0） |
| ライフ0の影響 | **スコア低下なし**（マルチ限定の仕様） |

**イベントポイント**（sekai.best 実装）:
```
(100 + floor(スコア ÷ 20000)) × 120
```
イベントボーナス・楽曲レート・ブーストのいずれも掛からない。

**チャレンジポイント**（note「チャレライ学基礎 Part.0」）:
```
200 + floor(スコア ÷ 8000)   （カラフルパス所持で ×2）
```

---

## 4. 未解決の最重要論点：スキル発動枠1〜5の決まり方

**情報源が真っ向から対立している。**

| 説 | 根拠 |
|---|---|
| **編成順で決まる（制御可能）** | `sekai-calculator` の `getSortedSkillDetails` が、スキルを scoreUp 昇順・楽曲側の `skill_score_solo` も昇順にソートして掛け合わせている。順序を選べる前提の実装 |
| **毎回ランダム抽選（制御不能）** | Game8・AppMedia・note複数。「**リタイアすると再抽選、リトライだと同じ順のまま**」という運用テクが広く共有されている |

**リタマラという慣行が存在すること自体が、ランダム説の状況証拠。** 制御できるなら引き直す必要がない。

**実機で編成順を入れ替えて発動順が変わるか見れば数十秒で決着する。未確認。**

**どちらでもツールの価値は変わらない。**
- 制御できるなら → 最適な並び順を出す
- ランダムなら → 「この抽選は当たりか、リタイアすべきか」を判定する

枠ごとの価値が数値で出ることが本体で、それは `skill_score_solo` から確定する。

---

## 5. 費用対効果の順序（実測ベース）

| 順位 | 施策 | 効果 |
|---|---|---|
| 1 | **総合力** | スコアに**完全比例**。1%上げれば1%上がる |
| 2 | **エリアアイテムのキャラ強化（最大+30%）** | ← チャレライ最強のテコ（後述） |
| 3 | 編成枠の解放（キャラランク22で5枠） | 枠＝スキル発動回数 |
| 4 | 最高倍率をリーダーに | アンコール確定＝実質2回発動 |
| 5 | 曲選び | 数% |
| 6 | 枠順の厳選 | **0.7%**（6パターン実測） |

### エリアアイテムがチャレライで突出する理由

**編成5枚が全員同一キャラ**なので、**キャラ強化アイテム（最大+30%）が5枚全部に満額で乗る**。通常編成ではキャラがばらけるためここまでの効率は出ない。**同キャラ縛りという制約が、ここでは逆に最大の武器になる。**

※ ミクのみキャラ強化が5セカイに分散しており、+30%相当に到達する手間が5倍。

### 枠順厳選のリターン実測

ブレス・ユア・ブレスMASTERで発動順6パターンを実測（アーラのプロセカブログ）:

| 発動順 | スコア |
|---|---|
| 120-120-100-130-140 | 1,988,000 |
| 100-130-120-140-120 | 1,974,160 |

**最良と最悪の差は13,840点＝0.7%。** リタイアを何十回も回すより総合力を1%上げるほうが確実。「発動順厳選が重要」は費用対効果を過大評価した通説。

---

## 6. スキル倍率（スキルレベル最大時）

| 種別 | 最大% | 条件 |
|---|---|---|
| ブルフェス限定（特訓後） | 160% | キャラランク100が必要 ※単一ソース、要検証 |
| バーチャルシンガー限定（ユニット一致） | 150% | ※チャレライで発動するか**要検証** |
| カラフェス限定（ライフ依存） | 140% | ライフ1000で最大 |
| カラフェス限定（判定依存） | 140% | GREAT以下を出すと低下 |
| スペシャル（★4） | 130% | PERFECT時のみ |
| 純スコアアップ（★4） | 120% | 無条件 |
| 判定強化+スコア（★4） | 100% | 持続5.5〜7秒 |
| ライフ回復+スコア（★4） | 100% | — |
| ★3 純スコア | 80% | — |
| バースデー/アニバーサリー | 80% | 回復850 |

**チャレライ固有の評価:**
- **ライフ依存140%が実質最強**。満タンスタートなのでノーミスなら実質無条件140%
- **回復スキルはほぼ無価値**（ライフ0でもスコアが落ちないため）。**バースデーカードは回復850が強くてもスコア80%＝★3相当**
- 判定強化は倍率100%と低いが、GREAT（0.7倍）潰しの保険にはなる

---

## 7. 潰れた通説

| 通説 | 実態 |
|---|---|
| **ノーツ数が多い曲ほど高スコア** | 分母で正規化されて消える。実測でも同Lv29でノーツ最少のビバハピが最高スコア |
| **「内部値・実効値」を上げろ** | **マルチ専用の概念。チャレライには一切適用されない。** 各カードが自分の倍率で独立に発動する |
| **枠順の厳選が重要** | 実測で0.7%。総合力1%のほうが確実 |
| **巻き込みを狙え** | 猶予 **15.77ms**（1フレーム未満）で実質不可能。初中級者は完全に無視してよい |
| **難易度は高いほど良い** | レベル係数は (Lv−5)×0.5% と緩やか。**ミス1回のコスト（約2%）がMASTER昇格の利益（+1〜3%）を上回る**。FCが安定する最高難易度を選ぶのが正解 |
| **総合力40万を目指す** | 全カンストでも36〜37万が上限 |
| **リトライで順を引き直す** | **リトライは順序が変わらない。引き直すのはリタイア。** |

---

## 8. 楽曲比較の実データ（2026-07-30 時点）

総合力250,000・全枠スコアアップ120%・AP前提で `music_metas.json` から算出。

### EXPERT 上位

| 順 | 曲 | スコア | base_score | skill計 | 曲長 |
|---|---|---|---|---|---|
| **1** | **レム** | **1,714,437** | 1.106 | **0.507** | 95.0s |
| 2 | ジャックポットサッドガール | 1,668,964 | 1.136 | 0.444 | 83.5s |
| 3 | 独りんぼエンヴィー | 1,663,575 | 1.110 | 0.461 | 74.8s |
| 4 | ビバハピ | 1,662,909 | 1.122 | 0.450 | 94.0s |

### MASTER 上位

| 順 | 曲 | スコア |
|---|---|---|
| 1 | Polar Star | 1,748,569 |
| 2 | ビバハピ | 1,728,322 |
| 3 | サンドリヨン 10th Anniversary | 1,726,691 |
| 18 | レム | 1,675,808 |
| 22 | 独りんぼエンヴィー | 1,669,107 |

### レム（765）が EXPERT 1位になる理由

スキル枠の重み `skill_score_solo` を並べると構造がはっきりする。

| | 1枠 | 2枠 | 3枠 | 4枠 | 5枠 | 6枠(リーダー) | 合計 |
|---|---|---|---|---|---|---|---|
| エンヴィー EXP | 0.069 | 0.071 | 0.076 | 0.078 | 0.063 | 0.104 | 0.461 |
| **レム EXP** | **0.010** | 0.068 | 0.078 | **0.102** | **0.119** | **0.130** | **0.507** |

**レムは後半3枠が異常に厚く、1枠目がほぼ死んでいる**（0.010＝1%）。「弱いスキルを1枠目に捨てて強いスキルを後半に集める」という定石が最も刺さる譜面。合計でもエンヴィーを10%上回る。

さらに**ノーツ密度が低い**（レム 4.84 notes/秒 vs エンヴィー 7.27 notes/秒＝1.5倍）ため、実プレイでは精度差でさらに開くと考えられる。

### 効率の軸は3つに分かれる

| 用途 | 律速 | 最適 |
|---|---|---|
| **イベラン周回** | 時間（回転数） | **独りんぼエンヴィー**（基礎点100 ÷ 74.8秒で圧倒的） |
| **チャレライ EXPERT** | 1回のスコア | **レム** |
| **チャレライ MASTER** | 同上 | **Polar Star** |

---

## 9. 既存ツールの状況（車輪の再発明を避けるため）

**日本語圏に編成最適化ソルバーは存在しない。** 日本語ツールはGoogleスプレッドシートと計算機止まりで、探索・最適化は英語圏と中国語圏が担っている。

| ツール | 圏 | チャレライ対応 | ライセンス | 備考 |
|---|---|---|---|---|
| **xfl03/sekai-calculator** | — | **あり**（`challenge-live-deck-recommend.ts`） | **LGPL-2.1** | 事実上の標準エンジン。npm公開。UIなし |
| Sajii (jiiku.dev) | 英 | あり | — | **作者自ら「確率的で最適解を保証しない」と明記** |
| Moesekai (pjsk.moe) | 中 | あり | **AGPL-3.0** | **作者自ら「AI生成コード多用でバグの可能性」と警告** |
| allium-deck | — | あり（枝刈りなし全探索） | MIT/Apache-2.0 | Rust製。最も新しく厳密 |
| sekai.best (Sekai Viewer) | — | **なし**（手動編成のみ） | GPL-3.0 | Music Recommender はAP固定・ボーナス0%固定の理論値 |

**xfl03 本人のフロントエンド 33Kit は死んでいる**（`33kit.xyz` が GoDaddy の売却ページに転じているのを確認）。つまり **`sekai-calculator` の実用フロントエンドが実質的に空席**。

**`api.sekai.best` はスコア計算に使えない。** OpenAPI spec を実際に取得して確認したところ、**全エンドポイントがイベントランキング専用**で、カード・スキル・楽曲のエンドポイントは存在しない。マスタデータは GitHub Pages の静的JSON（CORS全開・APIキー不要）から取る。

### ライセンス上の注意

- **`sekai-master-db-diff` には LICENSE も README も存在しない**（実測で両方404）。中身はゲームから抽出したセガ／Colorful Palette の著作物であり、**リポジトリ所有者に許諾を出す権限自体がない**。「公開されているから自由に使える」は成立しない
- **sekai.best ですら「研究目的のファンメイド、公式とは無関係」と明示**している。同水準の免責表記が最低ライン
- **AGPL-3.0（Moesekai / sekai-public）は絶対に触れない**。ネットワーク越しの提供でもソース公開義務が発生する
- `sekai-viewer` は GPL-3.0 で**参考は可、コピー不可**
- `sekai-calculator` は LGPL-2.1 で**npm依存として使う分には安全**
- `music_metas.json` は sekai.best の自前サーバー配信。**直リンクは帯域のタダ乗り**になるため、取得して自リポジトリに保存する現行の方式（`refresh-music-data.js`）が正しい

**公式ガイドラインの確認結果**（`pjsekai.sega.jp`）:
- [二次創作利用に関するガイドライン](https://pjsekai.sega.jp/guideline/index.html)は存在するが、**対象はファンアート・同人誌・コスプレ等の創作物**で、個人による非営利であることが条件
- **ゲームデータの抽出・再配布・ツール制作を明示的に許可した条項は見つからない**（＝黙認されている領域であって、許諾されているわけではない）
- 実務上の線引き: ①データを自前で再ホストしない ②画像アセットは数値データより慎重に ③非営利・広告なし ④「非公式ファンツール。セガ／Colorful Palette とは無関係」の免責表示（sekai.best 等が実際にやっている慣行）

---

## 9.5 実装時の落とし穴（一次ソースを読んで判明）

### base_score の中身は分解できる（実測検証済み）

```
base_score = Σ(ノーツ係数 × コンボ係数) ÷ Σ(ノーツ係数) × 難易度係数
```

検証: 曲49 master（Lv35・1666ノーツ）→ コンボ係数の加重平均 1.0670 × `ingamePlayLevels[35]=1.15` = **1.2271**、実データ `base_score = 1.2277`（**誤差0.05%**）。曲1 easy でも一致。

**つまり `base_score` には難易度係数が既に畳み込まれている。**

### `sekai-calculator` の精密版は難易度係数を掛けていない

`live-exact-calculator.ts`（1ノーツ単位）は `ingamePlayLevels.scoreCoefficient` を掛けていない。一方 `base_score`（高速版）には含まれている。**2つの実装は同じ値にならない。** 精密版を使うなら難易度係数を自分で掛ける必要がある。

### 最適スキル順は並べ替え不等式で求まる

`sekai-calculator` の実装:
1. カードのスキル効果を**昇順**ソート
2. `skill_score_solo` の先頭 `cardLength` 個も**昇順**ソート
3. ペアリング

**並べ替え不等式により、これが編成順を入れ替えて到達できる最大値**になる。6枠目はC位固定。

→ **この実装は「編成順で発動順が決まる」前提**であり、第4節の対立において「制御可能」説を支持する材料になる。

### マスタデータのフィールド名にtypoがある

`ingameNoteJudges.json` のフィールドは **`ingameNoteJadgeType`**（Judge の綴り間違い）。ゲーム側のtypoがそのまま出ているので、実装時に注意。

### 総合力計算には `Math.fround` が要る

ゲーム本体が**単精度浮動小数**で計算しているため、`Math.fround()`（float32 への丸め）を再現しないと1〜数点ずれる。エリアアイテムは3次元それぞれで計算 → 各次元 floor → 合算、という丸めの順序も守る必要がある。

### `skills.json` は22件しかない

全カードが22種類のスキル定義を共有し、`cards.json.skillId` で参照する構造。カードごとにスキルが定義されているわけではない。

`skillEffectType` の種類:

| 値 | 意味 |
|---|---|
| `score_up` | 通常のスコアアップ |
| `score_up_condition_life` | ライフ条件 |
| `score_up_keep` | 効果継続型 |
| `life_recovery` | ライフ回復 |
| `score_up_character_rank` | ブルフェス覚醒後（キャラランク閾値） |
| `other_member_score_up_reference_rate` | ブルフェス覚醒前（**他メンバーのスキルを参照**） |
| `score_up_unit_count` | 編成のユニット数で変動 |

### 目標逆算に使えるデータ

- **`challengeLiveHighScoreRewards.json`**（3146件）: `{characterId, highScore, resourceBoxId}` — **ハイスコア報酬の閾値表**
- **`playLevelScores.json`**（280件）: `liveType: "challenge_live"` を含む **S/A/B/C ランクのスコア閾値**。例) Lv1 → S:1,040,000 / A:840,000 / B:400,000 / C:4,000

**この2つを組み合わせると「次の報酬まであと何点」が出せる。既存ツールが誰もやっていない。**

### 実装の最大のハードルは手持ちカードの入力

`sekai-calculator` は `userCards`（カードID・レベル・覚醒・マスターランク・スキルレベル・エピソード解放）、`userCharacters`（キャラランク）、`userAreaItems`、`userHonors` を要求する。既存ツールは**ゲームAPIから吸い出す（規約的にアウト寄り）か、手入力させるかの二択**。ここのUX設計が成否をほぼ決める。

### 想定アーキテクチャ

```
計算  : npm i sekai-calculator（LGPL-2.1・依存として使う。フォークして中身を書き換えない）
データ: DataProvider を1つ実装するだけで丸ごと動く
        getMasterData(key) → sekai-world.github.io/sekai-master-db-diff/${key}.json
        getMusicMeta()     → storage.sekai.best/sekai-best-assets/music_metas.json
        getUserData(key)   → localStorage / IndexedDB
```

`cards.json` は **34MB** あるので初期ロード設計は必須。

---

## 10. 未検証・要確認

- **スキル発動枠1〜5が編成順かランダムか**（第4節）← 実機で数十秒
- **バーチャルシンガー限定「ユニット一致150%」がチャレライで発動するか**（同一キャラ5枚でユニット判定がどう働くか）
- **ブルフェス限定・特訓後160%（キャラランク100条件）の正確な仕様**（単一ソースのみ）
- セガ／Colorful Palette の二次創作ガイドライン本文（未取得）
- 日本語圏の個人製ツールの網羅調査（GitHub外に散在しており未完）

---

## 参照

**一次データ**
- マスタDB: `https://sekai-world.github.io/sekai-master-db-diff/*.json`（CORS全開、1日数回更新、413ファイル）
- 楽曲メタ: `https://storage.sekai.best/sekai-best-assets/music_metas.json`（2.3MB、`base_score` / `skill_score_solo[6]` / `event_rate` / `music_time` / `tap_count`）
- 実装: [xfl03/sekai-calculator](https://github.com/xfl03/sekai-calculator)（`live-calculator.ts` / `live-exact-calculator.ts`）

**有志検証（日本語で最も踏み込んでいる）**
- [チャレライ学基礎 Part.0〜6](https://note.com/notnishikori_18) — 曲別スキル位置の採譜、判定幅の実測、巻き込みの定量化
- [tomo3021「スコア計算式」](https://note.com/tomo3021/n/nf8cc535fdefb)
- [アーラのプロセカブログ「スキル順スコア比較」](https://ameblo.jp/renapl/entry-12745228151.html) — 6パターン実測
- [思い付きおきば](https://nanaev.hatenablog.jp/entry/2022/02/20/185225)
- [エンタメ中毒「効率曲ランキング2025」](https://entertainment-holic.com/pjsekai-highest-scoring-song-ranking-2025/) — 総合力255,471固定での実測

**攻略サイト（補助）**
- [Game8 チャレンジライブ](https://game8.jp/pjsekai/586801) / [AppMedia 効率曲](https://appmedia.jp/pjsekai/77511649) / [Gamerch スコア計算](https://gamerch.com/pjsekai/209946)
