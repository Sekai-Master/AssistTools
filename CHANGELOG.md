# 更新履歴

Sekai-Master の変更履歴です。書き方の規則とリリース手順は [docs/versioning.md](docs/versioning.md) を参照してください。

- バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。
- **カード・楽曲データの自動更新ではバージョンを上げません。** データの鮮度はサイトのフッターにある「データ更新」の日付を見てください。
- このファイルがサイトの [更新履歴ページ](https://sekaimaster.pages.dev/changelog) の正本です。ここを書き換えると、そのままページに出ます。
- **v1.9.0 より前の記録は、バージョン管理を始めた 2026-08-04 に Git の履歴から遡って起こしたものです。** 当時は版を切っていなかったため、まとまった変更の単位で番号を振り直しました。この期間の不具合修正はそれぞれの版の中に畳んであり、`1.8.1` のような修正版は存在しません。v1.9.0 以降は [docs/versioning.md](docs/versioning.md) の規則どおりに、修正だけのリリースにも番号が付きます。
- **さらに前史があります。** 2024年に Google Drive 上で公開していた初代（サイト名「Sekai-Ruler」、スキル実効値計算機とついぼジェネレーターの2本）は `ver.0.2.2` などを名乗っていました。実装もホストも現行とは別物で、`v1.0.0` はそこからの連番ではなく、**React へ全面リライトした 2026-07-22 を起点に振り直したもの**です。

<!--
  ★ 書式を変えるときは src/pages/legal/changelogParser.ts も一緒に直すこと。
    ページはこの Markdown を解析して描画しているので、見出しの形を崩すと
    その版が丸ごとページから消える（テストで検出される）。
-->

## [1.22.1] - 2026-08-27

### 修正

- **リフレッシュゲージ計算機の説明行で、区切りの空白が詰まっていました。**「現在 62.4%/ この曲は1回で +0.20%」のように、スラッシュの左側に隙間が無い状態でした。

## [1.22.0] - 2026-08-27

イベントの**途中から**開いても、いまの状態をそのまま入れて計画できるようにしました。あわせて、休憩とマイセカイが「ポイントの入らない空白」だったのを直しています。

### 追加

- **ゲージに「次の回復まで ○分」を入れられるようになりました**（リフレッシュゲージ計算機・周回プラン）。ゲージの%だけでは、**30分ごとの回復タイマーがどこまで進んでいるか**が分かりません。これまでは「たった今プレイを止めたところ」として計算していたので、イベントの途中で開くと最大30分ぶん予定がずれていました。ゲーム内のゲージ画面に出ている残り時間を入れると、そこから先の休憩・回復が正しく並びます。
  - 入れると「この状態からの全回復まで ○時間○分」も出ます（100%からの6時間ではなく、**いまのゲージから**の時間です）
- **周回プランで、マイセカイ採取のポイントが積み上がるようになりました。** 総合力とイベントボーナスを入れると1メモリあたりの単価が出て、採取ブロックにも獲得ptが付きます。「全回収」ボタンで1回ぶん（約89.5メモリ）をまとめて入れられます。
  - **編成の値（総合力・ボーナス・スキルの内部値・点数時速・焚き数）を、ページ上部の「入力に反映」でまとめて呼び出せます。** 打ち直しは要りません。ここで直した値は、それぞれの欄の隣にある「取り込む」で編成へ戻せます
- **周回プランの休憩で、オートを回せるようになりました。** オートライブは**リフレッシュゲージが増えない**ので、休憩の価値（ゲージの回復）を落とさずにポイントだけ稼げます。回す曲・難易度・焚き数を選ぶと1回のPtを計算します（実測値があれば手入力が優先されます）。
  - **回せない設定は理由付きで弾きます。** ①休憩の長さに入りきらない回数 ②1日の回数上限（通常10回／カラフルパス PRECIOUS で99回）③**4:00 のリセットまたぎ**——またいだぶんは翌日の回数を先に使い、前日の残りは消えます
- **編成ビルダーで、取り込んだ編成をツールから呼べるようにできます。** バックアップを別の端末で取り込んだとき、編成ビルダーには編成が並ぶのに、周回プランやアナライザーの「編成」からは1件も呼べない状態になっていました（保存された編成が持っているのはカード5枚の並びだけで、総合力・ボーナス・スキルの内部値は別に書き出されるため）。呼べない編成があるときは編成ビルダーの上に件数が出て、ボタン1つでまとめて登録できます。編成を呼び出したときにも自動で登録されます。
- **知見ノート（`/knowledge`）を作りました。** 計算の前提や実測の根拠を、引用できる文章として置いておく場所です。最初の3本は「1枠補正と巻き込み」「効率難易度の2つの意味」「1周のロスの実測根拠」。フッターから開けます。

### 変更

- **マイセカイ採取の入力を「スタミナ」から「メモリ」に変えました。** ポイントの単価がメモリ基準（木・石=1.0 / キラキラ・樽=0.5 / 草花=0.2）なので、そのまま入れられるようにしています。以前に保存したプランは自動で読み替わります。
- **効率曲ランキングに、順位の前提を書きました。** ここの順位は**1枠補正と巻き込みを含みません**。スコアの式はスキルの発動位置を平均で置いているので、1枠固定発動の効果も、判定の取り方で範囲をずらす巻き込みも表現できません。知っている人には順位が違って見え、知らない人は低難易度が強いと誤解するので、画面に明記しました。
- **「効率難易度検索」を取り下げました。** 全707曲を総当たりしたところ、683曲は最上位の難易度がそのまま最良で、答えが「APPENDがあればAPPEND、無ければMASTER」にしかなりません。意味のある版を作るには別のデータが要るので、準備中のカードを置き続けるのをやめました。

### 修正

- **編成へ「取り込む」ときに、確認が出るようになりました。** 「入力に反映」（編成 → ツールの入力）と「取り込む」（ツールの入力 → 編成）は向きが逆で、取り違えると編成の値が上書きされます。何がどう変わるか（例: イベントボーナス 821 → 927%）を出してから書き込むようにしました。
- **「取り込む」で、空欄にしていた項目の値が編成から消えていました。** 入力欄が空なのは「まだ入れていない」であって「消してほしい」ではないので、空欄の項目は書き込まないようにしました。
- **協力ライブのポイントが、実際より少し低く出ていました。** 他の4人のスコアで上がるぶんの上限が古い値（13）のままでした。実機で測って **16** に直しています（2026-04-30 の変更に追随できていませんでした）。効率曲ランキングの**順位は変わりません**（全曲に同じだけ乗るため）。
- **オート1回にかかる時間を、実測の 42.6秒 に直しました**（曲の長さ以外の部分）。これまでの33秒は根拠のない値で、99回まわす所要が実際（約6.1〜6.3時間）より短く出ていました。
- **周回プランのタイムラインが、スマホの幅で読めなくなっていました。** 1行に詰め込みすぎて、曲名や注意書きが1文字ずつの縦書きに潰れていました。狭い画面では折り返すようにしています。

## [1.21.0] - 2026-08-21

### 変更

- **トップページのツール一覧を5つに仕分けました。** ツールが12個になって、同じ大きさのカードが色違いで並ぶだけの状態になっていたので、**編成 → 計画 → 周回 → イベント → 交流・記録**の順に分けています。上から読むとイベランの流れそのものになります。
  - **色に意味を持たせました。** 仕分けごとにユニットカラーを1色ずつ（編成=Leo/need、計画=MORE MORE JUMP!、周回=Vivid BAD SQUAD、イベント=ワンダーランズ×ショウタイム、交流・記録=25時、ナイトコードで。）。**各ツールのページの色も、その仕分けの色に変わります**
  - VIRTUAL SINGER の水色は、利用規約・プライバシーポリシー・更新履歴に使っています
  - カードの説明を短く畳んで高さを揃えました。スマホでは一覧の縦の長さが約2割短くなります

### 修正

- **画像として書き出したとき、色が付いていませんでした。** 「周回プラン」「リフレッシュゲージ計算機」「必要稼働時間計算」「ポイント調整アナライザー」の4つで、ポイントなどの数字が**ツールの色ではなく黒**で描かれていました（2026-08-01 の配色切り替え対応以降）。
  - あわせて、**画像の中の数字が薄すぎて読みにくい問題**も直しました。明るいユニットカラー（水色・緑・橙・赤）は画像の背景に対してコントラストが足りないので、色味は保ったまま濃さだけ調整して描きます
- **「必要稼働時間計算」で書き出した画像に、「リフレッシュゲージ計算機」と書かれていました。**
- **緑の見出し帯の文字が読めませんでした。** 「ポイント調整アナライザー」の見出しが白文字で、背景の緑に対してほとんど判別できない状態でした。緑の帯には濃い文字を載せます。

## [1.20.0] - 2026-08-21

### 追加

- **「周回ラップ計測」に「終了して保存」を付けました。** 押すとその回の計測が1つの記録として閉じ、**開始時刻と終了時刻**つきで残ります。あとから一覧で見比べたり、1件ずつ／まとめて JSON で書き出せます（最大50件、古いものから消えます）。
  - 開始・終了の時刻は**最初と最後のタップ**です。最後の1周が終わってから「終了」を押すまでの間は測っていないので、記録の長さには入れていません

### 変更

- **「周回ラップ計測」の曲選びを一番上に移しました。** 決めたあとは折りたたんで、曲名・長さ・焚き数・単価が1行で見える状態にできます。
- **ライブの種類の切り替えを無くし、協力ライブ固定にしました。** 周回はまず協力ライブなので、選択肢が1つ増えるだけになっていました。

## [1.19.0] - 2026-08-21

### 追加

- **「周回ラップ計測」を追加しました。** 1周終わるたびにボタンを押すだけで、実際のラップ・**オーバーヘッド（曲以外にかかっている秒数）**・周/時が出ます。イベント214の周回ペースを測るために作った使い捨てのページを、曲を選べる形にして正式なツールにしたものです。
  - **休憩・部屋落ち・離席は「中断」ボタン**。中断していた時間は自動で平均から外れます（順番を間違えると平均が数倍に化ける、という操作を無くしました）
  - 押し忘れた区間は一覧の ＋ で周回数を増やせます。区間の時間をその数で割った値がラップになるので、**時刻の整合は壊れません**
  - 曲は検索から選べます。ライブの種類（協力・オート・チャレンジ）ごとに、**このサイトが見積りに使っている秒数とあなたの実測を並べて**表示します
  - 1周の獲得ポイントと焚き数を入れると実測の時速ポイントが出ます。そのまま編成に取り込めば、**必要稼働時間計算と周回プランがあなたの実測値で計算します**
  - 計測中は画面の自動ロックを抑止します（対応端末のみ）。記録はこの端末にだけ保存され、リロードしても続きから測れます

## [1.18.0] - 2026-08-20

### 修正

- **編成ビルダーが、ワールドリンクの「属性ボーナス」を計算に入れていませんでした。** ワールドリンクでは編成に含まれる属性の種類が多いほどボーナスが増えます（3種類で75%・4種類で100%・**5種類で125%**）。これが丸ごと抜けていたため、**開催中のワールドリンクでは最大125%ぶん低く出ており、おすすめする編成も間違っていました**。
  - 属性ボーナスは<b>編成全体に一度だけ</b>乗るので、カードごとの内訳とは分けて表示します
  - 5種類そろっていないときは、いま何種類で何%か、そろえるといくつになるかを画面に出します
  - ワールドリンク以外のイベント、チャレンジライブ、カスタム条件では乗りません

## [1.17.1] - 2026-08-19

### 修正

- **共有リンクを開いても、受け取ったリストが1件も出ないことがありました。** 「持っていて未回収の会話がある」を有効にしたまま開くと、渡された家具が全部消えていました。v1.16.0 で一度直した問題を、v1.17.0 の絞り込み追加で作り直していたので、**同じ抜け漏れが起きない形**に直してあります。
- **会話が連動することを画面に出すようにしました。** 同じ会話が起きるほかの家具の名前を、印を付ける前に表示します。知らないうちに別の家具の印が変わるのは怖いので。
- 共有リンクを開いた初見の人への案内が、**画面に無いアイコンを指していました**（「箱アイコン」と書いてありましたが、押す前は ＋ です）。
- **受け取ったリストを見ている間、絞り込みが効かないのに操作でき、しかも保存されていました。** あとで普通の図鑑に戻したとき、覚えのない絞り込みが効いてしまう状態でした。見ている間は絞り込みを隠します。
- 「ほしい」で絞ったときに、説明が「所持で絞りません」、条件の表示が「持っていない」と**嘘をついていました**。
- **絞り込みと並び順の文字が、スマホで単語の途中で折れていました**（「持って／いない」「ゲー／ム内」など）。ラベルを短くし、詰めて表示するようにしました。
- 「ほかの人だけで起きる会話」で、**全員の名前が薄くなって読めませんでした**。
- 「置いてあげられる」の印が、**ダークテーマで浮いて見えていました**。
- 並び順に説明を出すようにしました。「ゲーム内」が何の順か分かるようになります。
- **共有リンクが壊れていたときに、何も言わず普通の図鑑を開いていました**。壊れていることを伝えます。
- **共有リストをリンクの「#」以降に載せるようにしました。** この部分はサーバーに送信されないため、リストが通信の記録に残りません。これまでのリンクもそのまま開けます。
- 実在しない家具IDが入ったリンクで、件数だけが多く表示されることがないようにしました。
- 古いタブが開いたままのときに、ほしいものリストが消える可能性があったのを直しました。

## [1.17.0] - 2026-08-19

### 追加

- **同じ会話が複数の家具で起きる場合、片方に印を付けるともう片方にも付くようにしました。** ソファ・オーディオ・花壇・彫刻の左右など、別の家具でも中身が同じ会話があります（例: ロマンティックガーデンの彫刻の右と左）。片方で見たのに、もう片方が未回収のまま残る問題を解消しました。
  - **見ていない会話まで既読になることはありません。** ソファ類は共通の会話に加えて各自だけの会話も持つので、共通が1つあるだけでは連動させません。**中身がぴったり同じ組み合わせだけ**を連動させています（179組・家具29件）
- **並び順に「ゲーム内」を追加しました。** キャラクターランクの家具一覧と同じ順に並びます。ゲームの画面と見比べながら消し込めます。
- **「持っていて未回収の会話がある」で絞り込めるようにしました。** 持っている家具と見た会話の登録が一段落したあと、**次にどれを置けばいいか**がここに出ます。
- 家具の詳細で、**ほかの人だけで起きる会話も開いて印を付けられる**ようにしました。これまでは件数が出るだけで、印を付けるにはキャラの選択を外して開き直す必要がありました。

### 修正

- 一覧の「持っている」と「ほしい」のボタンが近すぎて押し間違えやすかったのを直しました（間隔を広げ、右に余白を入れました）。
- 「ほしい」ボタンを追加したぶん幅が減り、家具の名前が「鮮やかなユニゾン…」のように切れて見分けが付かなくなっていたのを直しました（2行まで表示します）。

## [1.16.0] - 2026-08-18

### 追加

- **マイセカイ図鑑に「ほしいものリスト」を追加しました。** 持っていない家具は他人のマイセカイへ模写しに行くしかないので、**何が足りないかを相手に伝えられないと取りに行けません**。一覧とモーダルのハートを押すとリストに入り、**共有リンクをコピーして渡せます**。
  - **リンクを開くと、受け取ったリストがそのまま一覧に出ます。** このサイトを使ったことがない人でも、渡された家具をすぐ見られます
  - **リンクを開いた人の画面では、自分が持っていて、送り主がほしがっているものに「置いてあげられる」の印が付きます。** それをマイセカイに置いておけば、相手が来て模写できます。まだ「持っている」の印を付けていない人には、何をすればいいかを画面で案内します
  - 絞り込みに「ほしい」が増えました。入れたものだけを見られます
  - **リストはリンクそのものに入っています。サイトには保存しません。** 持っている家具・見た会話・絞り込みの設定は**リンクに含まれません**し、受け取った側の情報が相手に伝わることもありません
  - リンクを閉じれば受け取ったリストは消えます。100件入れてもリンクは200文字ほどで、そのまま貼れます

## [1.15.1] - 2026-08-18

### 修正

- **チャレンジライブに総合力の上限が掛かっていました。** チャレンジライブはイベントの外なので上限は関係ありませんが、効率曲ランキングのチャレンジライブのタブでも総合力を336,000で頭打ちにしていました。総合力が33.6万を超えている人には、**スコアが実際より約16%低く出て、曲の順位も狂っていました**。さらに「上限のスイッチを入れてください」という、正しい値を見ている人を誤りへ誘導する警告まで出ていました。
- **ダークテーマで注意書きが読めませんでした。** 総合力の上限まわりの注意書き4か所が、暗い背景に暗い文字で出ていました（コントラスト比 1.9:1 程度）。**総合力が33.6万を超えている人＝この機能が必要な人ほど読めない**状態でした。
- 上限のスイッチを**入れたまま**ワールドリンクが終わると、上限のないイベントでも頭打ちのまま計算が続き、実際より低いポイントが出ていました。開催中でないのに上限を効かせているときも知らせるようにしました（切ったまま忘れたときの警告と対になります）。
- ライブボーナスと**オートの残り回数が同時に尽きた**とき、「ライボ切れ」と表示していました。この案内は「石やドリンクで注ぎ足す」ことを勧めますが、**回数が尽きているので石を割っても1回も増えません**。回数上限の方を表示するようにしました（ライボ25・5焚き・残り10回といった普通の組み合わせで起きます）。
- 「この時間で最大化」で、**どの曲でも残りオート回数を回り切ってしまう長さの時間**を入れたときに知らせるようにしました。この場合の並びは「1回あたりのPt」順と同じで、時間による最適化は効いていません。時間が効き始める長さも併せて出しています。
- 必要な石の数を目立たせました。4つ並ぶ数字のうち、**そこだけが実際に財布から出ていく数字**なので同じ見た目にしていました。
- 編成ビルダーで、イベントを選んだだけで全員に注意書きが出ていたのを、**編成が実際に上限を超えたときだけ**にしました。届いていない人には控えめな案内に変えています。
- 比較表の「上限」の印が総合力の桁揃えを崩していたのを直しました。

## [1.15.0] - 2026-08-18

### 追加

- **ワールドリンクの総合力上限に対応しました。** 開催中の「Again And Again Ambition！」を含むワールドリンク第3弾では、**編成の総合力が336,000を超えても、超えたぶんはスコアに乗りません**（ゲーム内の言い方では「発揮可能総合力」の制限）。これまでツールはこの上限を知らずに計算していたため、**総合力が33.6万を超えている人には実際より高いポイントを出し、おすすめする編成も間違っていました。**
  - **編成ビルダー**: 上限のあるイベントを選ぶと知らせます。比較表では上限に触れている編成に「上限」の印が付き、スコアと1回のPtは上限を掛けたあとの値になります。**カードの差し替え候補の並びも変わります** — 上限帯では「総合力が上がる差し替え」が1点にもならないので、これまでは損な差し替えを勧めていました
  - **効率曲ランキング**: 開催中なら自動で上限を効かせます。総合力はスコアに掛かる値なので、**並ぶ曲の順番そのものが変わります**。イベント期間外に下見したいときのために、**上限は手で入れたり外したりできます**（期間中は既定でオン、期間外は既定でオフ）。スイッチは「自分の条件で計算する」をオンにすると、総合力の入力欄の下に出ます
  - 表示している総合力そのものは丸めていません（ゲーム内の数字と突き合わせられるように）。頭打ちにするのはスコアの計算だけです
  - **ワールドリンクなら必ず上限が付くわけではありません。** 「水底に影を探して」「泡沫に抱かれて」などの過去13回には上限がなく、上限があるのは第3弾の5回だけです。ツールはイベントごとのデータを見て判断しています
  - 上限を手で切ったまま忘れないように、**開催中に上限を切っていると警告を出します**（設定は端末に残るので、次のワールドリンクが始まったときに切れたままにならないように）
  - なお、ポイント調整アナライザーは上限を適用していません。あちらで総合力が効くのはマイセカイの採取単価だけで、**そこにも上限が掛かるかを確かめられていない**ためです

## [1.14.0] - 2026-08-18

### 追加

- 効率曲ランキングのオート周回に「**この時間で最大化**」を追加しました。放置できる時間を入れると、**その時間内に稼げる合計イベントPtが多い順**に並び替わります。イベントランキングを走っていて、ライブボーナスの効率より**時間あたりをどこまでも詰めたい**ときに使えます（既定の時間は次の4:00までを自動で入れます。オートの回数は毎日4:00に消えるため）。
  - **並ぶ曲がまるで変わります。** 例えば PRECIOUS・5焚き・放置2時間で、既定の総合力25万・ボーナス400%のとき、1位は**独りんぼエンヴィー(EXPERT)**（66回・約131万Pt）になります。この曲は**通常のオート効率では3,677曲中3,658位**です。1回あたりの効率がほぼ最下位でも、短い（74.8秒）ぶん時間内に何度も回せるので勝ちます
  - 既定では「**ライボが足りなければ石やドリンクで注ぎ足す**」前提で計算します（この機能を使う場面ではライボ効率を度外視するため）。上の例なら**ライボ305・石にして3,050個**が要ります。この必要量は画面に出るので、割に合うか判断してから走ってください。手持ちだけで走る設定にも切り替えられます
  - 各行に**何回まわせるか**と、**何で止まるか**（時間切れ／ライボ切れ／オートの回数上限）が出ます。止まる理由で次の一手が変わります（回数上限なら焚き数を上げる、ライボ切れなら注ぎ足すか焚き数を下げる、時間切れなら短い曲に替える）
  - 焚き数0でも計算できるようになりました（これまで0回と出ていました）
- **なお、ふだんはこの並びを使わないでください。** ライブボーナスは翌日に持ち越せるので、イベント全体で見るなら「ライボがなくなるまで」の並び（1回あたりの効率）の方が正しい軸です。**手持ちのライボだけで走る場合、この機能はほとんど意味がありません** — 5焚き・ライボ25なら30分ほどで手持ちを使い切り、それより長い時間を入れても順位は通常のオートと同じになります。効くのは、石を注ぎ足してでも時間を詰めたい場面に限られます

## [1.13.0] - 2026-08-17

### 追加

- **マイセカイ リアクション図鑑**を追加しました。キャラを選ぶと、そのキャラが反応する家具を一覧できます。ゲーム内の絞り込みは**自分が設計図を持っている家具しか出ない**ため、まだ持っていない家具や、他人のマイセカイへ模写しに行く候補を探せませんでした。そこを埋めるツールです。
  - 反応は3種類あります。**固有会話**（その家具を置くと発生する専用の会話）、**キャラが動く**（家具を使うモーション）、**好みの家具**。種類でも絞り込めます
  - **模写できるかどうか**を出しています。「模写可」は他人のマイセカイで設計図を写せるもの、「設計図なし」は模写以外の入手方法しかないものです
  - **会話の本文は載せていません。** 出しているのは「誰の会話が何本あるか」までです
  - 既定では反応がある家具だけを表示します。全部の家具を見たいときは「反応がある家具だけ」をオフにしてください
  - **家具のサムネイル**を表示します（1,501枚）
  - 行をタップすると詳細が開き、**会話が起きる顔ぶれごとに「見た」印**を付けられます。「司ひとり」と「司＋類」は別の会話として数えます
  - **持っている家具に印**を付けられます。一覧の右端のボタンから切り替えられ、「持っている／持っていない」で絞り込めます
  - **ひとりで喋る会話が無い家具**（ユニットのソファなど）には「揃うと」が付きます。ひとりで訪ねても会話は始まりません
  - 会話の人数（ひとりで／複数人で）でも絞り込めます
  - 印と絞り込みはこの端末にだけ保存されます（設定画面から書き出し・削除できます）

## [1.12.0] - 2026-08-12

### 追加
- オート周回の「ライブボーナスがなくなるまで」に**今日の残りオート回数**の入力を追加しました。オートの上限（通常10回・PRECIOUS 99回）は**その日にまだ何回残っているか**で効くので、すでに何回か消化していると、まわせる回数はそのぶん少なくなります。既定はコースの上限が入ります。

### 修正
- 上記にともない、まわせる回数がオート回数で頭打ちになったときの表示を「オートの上限◯回で止まります」から「オートの残り◯回を使い切って止まります」に改めました。

## [1.11.2] - 2026-08-12

### 修正

- オート周回で、回数上限に達して止まったときの説明が「焚き数に足りないので止まります」と誤って出ていたのを直しました。回数上限で余ったぶんは**焚き数を上げれば使える**ので、意味が逆になっていました

## [1.11.1] - 2026-08-12

### 修正

- カラフルパスの選択肢を「なし／BASIC」に直しました。BASIC ではライブボーナスの上限もオートの回数も解放されないため、未加入と同じ扱いになります

## [1.11.0] - 2026-08-12

### 追加

- 効率曲ランキングのオート周回に「**ライボがなくなるまで**」の計算を追加しました。いま持っているライブボーナスを入れると、**何回まわせるか**・所要時間・獲得ポイント・使い切れずに残るライボが出ます。走っている間の自然回復も数えるので、単純な割り算より多くまわせます
- カラフルパス（なし／DELUXE／PRECIOUS）を選べるようにしました。ライブボーナスの上限（25／50）とオートの1日の回数（10／99）がこれで変わります

### 変更

- オート周回の「まとめて回すと」を、**ゲームのオート設定と同じ2つの入口**（ライボがなくなるまで／回数を指定）に組み直しました
- 「回数を指定」では、手持ちで足りないぶんを**石や回復アイテムでいくつ注ぎ足すか**を出すようにしました。オートの回数指定は、足りなくなっても石やドリンクで走り続けられるためです
- 残りが焚き数を下回ると止まる（5焚きで残り2なら回してくれない）ことを計算に入れ、**使い切れずに残るライボ**を必ず出すようにしました

## [1.10.0] - 2026-08-12

### 修正

- **総合力が実際より低く出ることがあったのを直しました。** ミクたちが他ユニットの衣装を着たカード（ユニット限定カード）で、エリアアイテムの効果を取り違えていました。こうしたカードはピアプロとその衣装のユニットの両方に属し、**効果が高いほうが乗ります**。実機の編成2つでゲーム内の表示と1ポイントの違いもなく一致することを確認しています
- 編成を比べる画面で、ライブの種類を変えても1曲あたりのロスが協力ライブのまま固定されていたのを直しました。オートで比べると所要時間が実際より短く出ていました

### 変更

- **「1曲あたりのロス」の既定値を周回の種類ごとに分けました。** オート周回は33秒、手動（協力ライブ）は54秒です。これまでどの種類でも20秒を使っており、特に手動の所要時間を短く見積もっていました。54秒は実測（独りんぼエンヴィーを28回/時）からの逆算で、周回プランが以前から使っていた値です
- 編成ビルダーで編成を保存すると、**編成プロフィールにも自動で反映される**ようになりました。効率曲ランキングや必要稼働時間からすぐ呼び出せます。これまでは保存とは別に「編成プロフィールへ反映」を押す必要があり、押し忘れると他のツールに出てきませんでした

## [1.9.2] - 2026-08-10

### 修正

- ページを開いた直後に表示がガクッとずれるのを直しました。読み込みが終わった瞬間に画面全体が下へ動いていたもので、編成ビルダーと効率曲ランキングで特に大きく出ていました

## [1.9.1] - 2026-08-04

### 修正

- 編成ビルダーとカード検索で、カード名の書き出し位置が行ごとにずれてガタついていたのを直しました。レアリティのバッジが「★4」と「Birthday」で幅が違うのが原因でした

## [1.9.0] - 2026-08-04

利用規約・プライバシーポリシー・この更新履歴ページを追加しました。あわせてバージョン表記を始めています。

### 追加

- 利用規約・プライバシーポリシー・更新履歴の3ページ。フッターからいつでも開けます
- フッターにアプリのバージョンとデータの更新日を表示
- アクセス解析（Cloudflare Web Analytics）。Cookie を使わず、個人を追跡しない方式のものです

### 変更

- フォントとアイコンをサイト自身から配信するようにしました。これまでは Google のサーバーから読み込んでいたため、閲覧しただけで Google に接続が発生していました。外部への通信はこれでゼロになります

## [1.8.0] - 2026-08-04

### 追加

- 編成プロフィールを「必要稼働時間計算」と「スキル実効値計算機」でも使えるようにしました。総合力やボーナスを入れ直す手間がなくなります
- 効率曲ランキングに曲の絞り込みと、ライブボーナス回復を考慮するトグルを追加

### 変更

- 時速まわりの計算を各ツールで共通化し、実測値からの逆算を扱えるように拡張しました

## [1.7.0] - 2026-08-02

編成ビルダーを追加しました。

### 追加

- **編成ビルダー**。カード5枚のイベントボーナスと総合力を計算し、編成を並べて最終イベントポイントで比較できます（ボーナスを落として総合力を盛った方が勝つ場合を見つけるためのものです）
- 編成の紹介カード（PNG）の書き出し。立ち絵・イベントロゴ・マスターランクを載せた 16:9 の画像を作れます
- 編成に入れていないカードも「持っている」として登録でき、候補の絞り込みに使えます
- 1枠だけ差し替えた候補の提示と、編成の複製
- チャレンジライブ編成への対応と、イベントボーナスの「カスタム」入力
- 設定画面に、保存データの書き出し・取り込みを追加。端末を移るときに使えます
- カード画像をサイト自身から配信するようにしました（表示が安定します）

### 修正

- 参照系スキルの上限がスキルレベルで変わることを計算に反映

## [1.6.0] - 2026-08-01

### 追加

- **ライト／ダークテーマ**。端末の設定に追随させるか、明示的に選べます
- **編成プロフィール**。総合力・ボーナスなどを1か所に置き、各ツールの初期値として共有します
- 一覧からツールへの遷移を、カードが見出しの形へ変形して繋がる動きにしました

### 変更

- 効率曲ランキングの並び替えを目で追えるようにし、順位の変化量を表示するようにしました

## [1.5.0] - 2026-07-31

### 追加

- 画面遷移の演出と、その強さを選べる設定画面。演出が重い・不要な場合は切れます
- 読み込みに失敗したときの復帰導線（再読み込みの案内）

## [1.4.0] - 2026-07-30

### 追加

- **効率曲ランキング**。手動周回・オート周回・チャレンジライブの3つで効率曲を順位表示します。用途で最適解が変わるので分けています
- 楽曲データに難易度別の譜面レベル・ノーツ数・スコア係数を追加

### 変更

- 楽曲マスタを毎日自動で更新するようにしました

## [1.3.0] - 2026-07-25

### 変更

- ポイント調整アナライザーの Step1〜3 を全面的に作り直しました。Step3 を3択に、Step2 を2モードに整理し、逆算の構造と結果画像を刷新しています
- 調整プランの提示を選択肢型にし、達成率が極端な値になる問題を解消しました
- ついぼジェネレーターを PC で2カラム、スマホで固定バーの形に変更
- ついぼジェネレーターに本文のコピーボタンを追加

## [1.2.0] - 2026-07-23

### 追加

- ポイント調整アナライザーにマイセカイ不使用モードを追加（複数回ライブ・曲側の探索まで対応）
- プランの採択 UI・曲選択・配信停止曲の除外
- 調整プラン画像にコピーとラストランを追加

### 変更

- スコア上限を設定できるようにし、調整プランを「回数 × ライブボーナス」のトレードオフとして提示するようにしました

## [1.1.0] - 2026-07-22

### 追加

- **リフレッシュゲージ計算機**。現在のゲージから 100% までの時間と、持続ペースを出します
- **必要稼働時間計算**。焚き数・稼働時間・編成ボーナスから到達ポイントを計算し、目標からの逆算もできます
- **周回プラン**。稼働・休憩・マイセカイのブロックを積んで、累積の到達ポイントと到達時刻を可視化します。名前を付けて保存でき、画像にも書き出せます

## [1.0.0] - 2026-07-22

素の HTML/CSS/JavaScript だったサイトを、React + TypeScript へ全面的に書き直しました。見た目と使い勝手を作り直し、計算部分にはテストを張ってあります。

### 追加

- **ついぼジェネレーター**（協力ライブ募集ツイートの作成。プレビュー・履歴・テンプレ付き）
- **スキル実効値計算機**（先頭スキル値・内部値からの実効値算出。逆算・ブルフェス個体に対応）
- **ポイント調整アナライザー**（目標ポイントへの調整プランの算出）
- **BINGO カードジェネレーター**（チアフルカーニバル用。シード値で再現できます）
